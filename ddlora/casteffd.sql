create table castefaf
(
  catetoe     varchar2(3) default ' ' not null,
  catedes     varchar2(35) default ' ' not null,
  catespa     varchar2(50) default ' ' not null,
  lf          varchar2(1) default ' ' not null,
constraint castefa1 primary key( 
catetoe)
)
tablespace pas_data 
enable primary key using index 
  tablespace pas_indx; 
