.
. PMSIKIIO
. --------
. Filename     : pmsikiaf.dat
.
RSPMIKI1  RESET     KEY37
          READ      PMSIKIA1,KEY37;;
          RETURN
.
RAPMIKI1  RESET     KEY37
          MOVE      ZERO,OVRCD
          READ      PMSIKIA1,KEY37;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RDPMIKI1  RESET     KEY37
          MOVE      ZERO,OVRCD
          READ      PMSIKIA1,KEY37;PMIKCODE,PMIKKKWD,PMIKSPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RKPMIKI1  MOVE      ZERO,OVRCD
          READKS    PMSIKIA1;PMIKCODE,PMIKKKWD,PMIKSPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RPPMIKI1  MOVE      ZERO,OVRCD
          READKP    PMSIKIA1;PMIKCODE,PMIKKKWD,PMIKSPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
UPPMIKI1  MOVE      ZERO,OVRCD
          UPDATE    PMSIKIA1;PMIKCODE,PMIKKKWD,PMIKSPAR
          GOTO      UPDFLOCK IF LOCKED
          RETURN
.
WRPMIKI1  RESET     KEY37
          WRITE     PMSIKIA1,KEY37;PMIKCODE,PMIKKKWD,PMIKSPAR
          RETURN
.
DEPMIKI1  RESET     KEY37
          DELETE    PMSIKIA1,KEY37
          RETURN
.
RLPMIKI1  RESET     KEY37
          MOVE      ZERO,OVRCD
          READL     PMSIKIA1,KEY37;PMIKCODE,PMIKKKWD,PMIKSPAR
          GOTO      OVERCOND IF OVER
          GOTO      OVERLOCK IF LOCKED
          RETURN
.
KLPMIKI1  MOVE      ZERO,OVRCD
          READLKS   PMSIKIA1;PMIKCODE,PMIKKKWD,PMIKSPAR
          GOTO      OVERCOND IF OVER
          GOTO      OVERLOCK IF LOCKED
          RETURN
.
PLPMIKI1  MOVE      ZERO,OVRCD
          READLKP   PMSIKIA1;PMIKCODE,PMIKKKWD,PMIKSPAR
          GOTO      OVERCOND IF OVER
          GOTO      OVERLOCK IF LOCKED
          RETURN
.
UUPMIKI1  RESET     KEY37
          UNLOCK    PMSIKIA1
          RETURN
.
RSPMIKI2  RESET     KEY37
          READ      PMSIKIA2,KEY37;;
          RETURN
.
RAPMIKI2  RESET     KEY37
          MOVE      ZERO,OVRCD
          READ      PMSIKIA2,KEY37;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RDPMIKI2  RESET     KEY37
          MOVE      ZERO,OVRCD
          READ      PMSIKIA2,KEY37;PMIKCODE,PMIKKKWD,PMIKSPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RKPMIKI2  MOVE      ZERO,OVRCD
          READKS    PMSIKIA2;PMIKCODE,PMIKKKWD,PMIKSPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RPPMIKI2  MOVE      ZERO,OVRCD
          READKP    PMSIKIA2;PMIKCODE,PMIKKKWD,PMIKSPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
UPPMIKI2  MOVE      ZERO,OVRCD
          UPDATE    PMSIKIA2;PMIKCODE,PMIKKKWD,PMIKSPAR
          GOTO      UPDFLOCK IF LOCKED
          RETURN
.
WRPMIKI2  RESET     KEY37
          WRITE     PMSIKIA2,KEY37;PMIKCODE,PMIKKKWD,PMIKSPAR
          RETURN
.
DEPMIKI2  RESET     KEY37
          DELETE    PMSIKIA2,KEY37
          RETURN
.
RLPMIKI2  RESET     KEY37
          MOVE      ZERO,OVRCD
          READL     PMSIKIA2,KEY37;PMIKCODE,PMIKKKWD,PMIKSPAR
          GOTO      OVERCOND IF OVER
          GOTO      OVERLOCK IF LOCKED
          RETURN
.
KLPMIKI2  MOVE      ZERO,OVRCD
          READLKS   PMSIKIA2;PMIKCODE,PMIKKKWD,PMIKSPAR
          GOTO      OVERCOND IF OVER
          GOTO      OVERLOCK IF LOCKED
          RETURN
.
PLPMIKI2  MOVE      ZERO,OVRCD
          READLKP   PMSIKIA2;PMIKCODE,PMIKKKWD,PMIKSPAR
          GOTO      OVERCOND IF OVER
          GOTO      OVERLOCK IF LOCKED
          RETURN
.
UUPMIKI2  RESET     KEY37
          UNLOCK    PMSIKIA2
          RETURN
.
