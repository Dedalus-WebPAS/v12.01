.
RALGCLI1 RESET     KEY12
         MOVE      ZERO,OVRCD
         READ      LEGCLIA1,KEY12;ANS
         GOTO      OVERCOND IF OVER
         RETURN  
.
RSLGCLI1 RESET     KEY12
         READ      LEGCLIA1,KEY12;;
         RETURN  
.
RDLGCLI1 MOVE      ZERO,OVRCD
         RESET     KEY12
         READ      LEGCLIA1,KEY12;LOCASITE,LOCACLIN,LOCADOCT,LOCADESC,LOCACCON:
                                  LOTCLIAC,LOCASPAR
         GOTO      OVERCOND IF OVER
         MOVE      LOCASITE,LSOCASIT
         MOVE      LOCACLIN,LSOCACLI
         RETURN  
.
RKLGCLI1 MOVE      ZERO,OVRCD
         READKS    LEGCLIA1;LOCASITE,LOCACLIN,LOCADOCT,LOCADESC,LOCACCON:
                            LOTCLIAC,LOCASPAR
         GOTO      OVERCOND IF OVER
         MOVE      LOCASITE,LSOCASIT
         MOVE      LOCACLIN,LSOCACLI
         RETURN  
.
RPLGCLI1 MOVE      ZERO,OVRCD
         READKP    LEGCLIA1;LOCASITE,LOCACLIN,LOCADOCT,LOCADESC,LOCACCON:
                            LOTCLIAC,LOCASPAR
         GOTO      OVERCOND IF OVER
         MOVE      LOCASITE,LSOCASIT
         MOVE      LOCACLIN,LSOCACLI
         RETURN
.
RALGCLI2 RESET     KEY6
         MOVE      ZERO,OVRCD
         READ      LEGCLIA2,KEY6;ANS
         GOTO      OVERCOND IF OVER
         RETURN  
.
RSLGCLI2 RESET     KEY6
         READ      LEGCLIA2,KEY6;;
         RETURN  
.
RKLGCLI2 MOVE      ZERO,OVRCD
         READKS    LEGCLIA2;LOCASITE,LOCACLIN,LOCADOCT,LOCADESC,LOCACCON:
                            LOTCLIAC,LOCASPAR
         GOTO      OVERCOND IF OVER
         RETURN  
.
RDLGCLI2 MOVE      ZERO,OVRCD
         RESET     KEY6
         READ      LEGCLIA2,KEY6;LOCASITE,LOCACLIN,LOCADOCT,LOCADESC,LOCACCON:
                                 LOTCLIAC,LOCASPAR
         GOTO      OVERCOND IF OVER
         RETURN
.
RALGCLI3 RESET     KEY12
         MOVE      ZERO,OVRCD
         READ      LEGCLIA3,KEY12;ANS
         GOTO      OVERCOND IF OVER
         RETURN
.
RSLGCLI3 RESET     KEY12
         READ      LEGCLIA3,KEY12;;
         RETURN
.
RKLGCLI3 MOVE      ZERO,OVRCD
         READKS    LEGCLIA3;LOCASITE,LOCACLIN,LOCADOCT,LOCADESC,LOCACCON:
                            LOTCLIAC,LOCASPAR
         GOTO      OVERCOND IF OVER
         RETURN
.
RDLGCLI3 MOVE      ZERO,OVRCD
         RESET     KEY12
         READ      LEGCLIA3,KEY12;LOCASITE,LOCACLIN,LOCADOCT,LOCADESC,LOCACCON:
                                 LOTCLIAC,LOCASPAR
         GOTO      OVERCOND IF OVER
         RETURN
.
RALGCLI4 RESET     KEY36
         MOVE      ZERO,OVRCD
         READ      LEGCLIA4,KEY36;ANS
         GOTO      OVERCOND IF OVER
         RETURN
.
RSLGCLI4 RESET     KEY36
         READ      LEGCLIA4,KEY36;;
         RETURN
.
RKLGCLI4 MOVE      ZERO,OVRCD
         READKS    LEGCLIA4;LOCASITE,LOCACLIN,LOCADOCT,LOCADESC,LOCACCON:
                            LOTCLIAC,LOCASPAR
         GOTO      OVERCOND IF OVER
         RETURN
.
RDLGCLI4 MOVE      ZERO,OVRCD
         RESET     KEY36
         READ      LEGCLIA4,KEY36;LOCASITE,LOCACLIN,LOCADOCT,LOCADESC,LOCACCON:
                                 LOTCLIAC,LOCASPAR
         GOTO      OVERCOND IF OVER
         RETURN
.
