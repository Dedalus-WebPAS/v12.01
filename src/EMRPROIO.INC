RSEMPRO1  RESET     KEY9
          READ      EMRPROA1,KEY9;;
          RETURN
.
RAEMPRO1  RESET     KEY9
          MOVE      ZERO,OVRCD
          READ      EMRPROA1,KEY9;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RDEMPRO1  RESET     KEY9
          MOVE      ZERO,OVRCD
          READ      EMRPROA1,KEY9;EMPRCODE,EMPRDESC,EMPRHDPE,EMPRSPAR
          GOTO      OVERCOND IF OVER
          RETURN
RLEMPRO1  RESET     KEY9
          MOVE      ZERO,OVRCD
          READL     EMRPROA1,KEY9;EMPRCODE,EMPRDESC,EMPRHDPE,EMPRSPAR
          GOTO      OVERCOND IF OVER
          GOTO      OVERLOCK IF LOCKED
          RETURN
.
RKEMPRO1  MOVE      ZERO,OVRCD
          READKS    EMRPROA1;EMPRCODE,EMPRDESC,EMPRHDPE,EMPRSPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RPEMPRO1  MOVE      ZERO,OVRCD
          READKP    EMRPROA1;EMPRCODE,EMPRDESC,EMPRHDPE,EMPRSPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
UPEMPRO1  UPDATE    EMRPROA1;EMPRCODE,EMPRDESC,EMPRHDPE,EMPRSPAR
          RETURN
.
WREMPRO1  RESET     KEY9
          WRITE     EMRPROA1,KEY9;EMPRCODE,EMPRDESC,EMPRHDPE,EMPRSPAR
          RETURN
.
DEEMPRO1  RESET     KEY9
          DELETE    EMRPROA1,KEY9
          RETURN
.
ULEMPRO1  UNLOCK    EMRPROA1
          RETURN
.
RSEMPRO2  RESET     KEY79
          READ      EMRPROA2,KEY79;;
          RETURN
.
RAEMPRO2  RESET     KEY79
          MOVE      ZERO,OVRCD
          READ      EMRPROA2,KEY79;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RDEMPRO2  RESET     KEY79
          MOVE      ZERO,OVRCD
          READ      EMRPROA2,KEY79;EMPRCODE,EMPRDESC,EMPRHDPE,EMPRSPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RLEMPRO2  RESET     KEY79
          MOVE      ZERO,OVRCD
          READL     EMRPROA2,KEY79;EMPRCODE,EMPRDESC,EMPRHDPE,EMPRSPAR
          GOTO      OVERCOND IF OVER
          GOTO      OVERLOCK IF LOCKED
          RETURN
.
RKEMPRO2  MOVE      ZERO,OVRCD
          READKS    EMRPROA2;EMPRCODE,EMPRDESC,EMPRHDPE,EMPRSPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RPEMPRO2  MOVE      ZERO,OVRCD
          READKP    EMRPROA2;EMPRCODE,EMPRDESC,EMPRHDPE,EMPRSPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
UPEMPRO2  UPDATE    EMRPROA2;EMPRCODE,EMPRDESC,EMPRHDPE,EMPRSPAR
          RETURN
.
WREMPRO2  RESET     KEY79
          WRITE     EMRPROA2,KEY79;EMPRCODE,EMPRDESC,EMPRHDPE,EMPRSPAR
          RETURN
.
DEEMPRO2  RESET     KEY79
          DELETE    EMRPROA2,KEY79
          RETURN
.
ULEMPRO2  UNLOCK    EMRPROA2
          RETURN
.
