.
. PATECMIO.INC
. ------------
. Filename       :  patecmaf
. Description    :  Episode Morbidity Changes File
. Variables      :  OVRCD     FORM      1
. Keys           :  KEY18
. Modifications  :
.
. Index 1 KEY18
.
RAPTECM1  MOVE      ZERO,OVRCD
          RESET     KEY18
          READ      PATECMA1,KEY18;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RSPTECM1  RESET     KEY18
          READ      PATECMA1,KEY18;;
          RETURN
.
RDPTECM1  MOVE      ZERO,OVRCD
          RESET     KEY18
          READ      PATECMA1,KEY18;DPTEMADM,DPTEMEPN,PTEMCDTE,PTEMNEWF,PTEMSPAR
          GOTO      OVERCOND IF OVER
          MOVE      DPTEMADM,PTEMADMN
          MOVE      DPTEMEPN,PTEMEPNO
          RETURN
.
RKPTECM1  MOVE      ZERO,OVRCD
          READKS    PATECMA1;DPTEMADM,DPTEMEPN,PTEMCDTE,PTEMNEWF,PTEMSPAR
          GOTO      OVERCOND IF OVER
          MOVE      DPTEMADM,PTEMADMN
          MOVE      DPTEMEPN,PTEMEPNO
          RETURN
.
RPPTECM1  MOVE      ZERO,OVRCD
          READKP    PATECMA1;DPTEMADM,DPTEMEPN,PTEMCDTE,PTEMNEWF,PTEMSPAR
          GOTO      OVERCOND IF OVER
          MOVE      DPTEMADM,PTEMADMN
          MOVE      DPTEMEPN,PTEMEPNO
          RETURN
.
UPPTECM1  
          MOVE      PTEMADMN,DPTEMADM 
          MOVE      PTEMEPNO,DPTEMEPN 
          UPDATE    PATECMA1;DPTEMADM,DPTEMEPN,PTEMCDTE,PTEMNEWF,PTEMSPAR
          RETURN
.
WRPTECM1  RESET     KEY18
          MOVE      PTEMADMN,DPTEMADM 
          MOVE      PTEMEPNO,DPTEMEPN 
          WRITE     PATECMA1,KEY18;DPTEMADM,DPTEMEPN,PTEMCDTE,PTEMNEWF,PTEMSPAR
          RETURN
.
DEPTECM1  RESET     KEY18
          DELETE    PATECMA1,KEY18
          RETURN
.
. Index 2 KEY18
.
RAPTECM2  MOVE      ZERO,OVRCD
          RESET     KEY18
          READ      PATECMA2,KEY18;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RSPTECM2  RESET     KEY18
          READ      PATECMA2,KEY18;;
          RETURN
.
RDPTECM2  MOVE      ZERO,OVRCD
          RESET     KEY18
          READ      PATECMA2,KEY18;DPTEMADM,DPTEMEPN,PTEMCDTE,PTEMNEWF,PTEMSPAR
          GOTO      OVERCOND IF OVER
          MOVE      DPTEMADM,PTEMADMN
          MOVE      DPTEMEPN,PTEMEPNO
          RETURN
.
RKPTECM2  MOVE      ZERO,OVRCD
          READKS    PATECMA2;DPTEMADM,DPTEMEPN,PTEMCDTE,PTEMNEWF,PTEMSPAR
          GOTO      OVERCOND IF OVER
          MOVE      DPTEMADM,PTEMADMN
          MOVE      DPTEMEPN,PTEMEPNO
          RETURN
.
RPPTECM2  MOVE      ZERO,OVRCD
          READKP    PATECMA2;DPTEMADM,DPTEMEPN,PTEMCDTE,PTEMNEWF,PTEMSPAR
          GOTO      OVERCOND IF OVER
          MOVE      DPTEMADM,PTEMADMN
          MOVE      DPTEMEPN,PTEMEPNO
          RETURN
.
UPPTECM2  
          MOVE      PTEMADMN,DPTEMADM 
          MOVE      PTEMEPNO,DPTEMEPN 
          UPDATE    PATECMA2;DPTEMADM,DPTEMEPN,PTEMCDTE,PTEMNEWF,PTEMSPAR
          RETURN
.
WRPTECM2  RESET     KEY18
          MOVE      PTEMADMN,DPTEMADM 
          MOVE      PTEMEPNO,DPTEMEPN 
          WRITE     PATECMA2,KEY18;DPTEMADM,DPTEMEPN,PTEMCDTE,PTEMNEWF,PTEMSPAR
          RETURN
.
DEPTECM2  RESET     KEY18
          DELETE    PATECMA2,KEY18
          RETURN
