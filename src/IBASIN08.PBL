.-------------------------------------------------------
          INC       STDGENDF     * Required Include
.
VERSION   INIT      "V12.01.00"
.-------------------------------------------------------
. V9.02.00  03.05.2002  Glenn Saunders
.           Export to new release i.e. v9.02 from vf.09
.-------------------------------------------------------
. VF.00.01  10.11.1999 Charles Handaya
.           Fix Y2K of Audit Files Date (CCYYMMDD)
.-------------------------------------------------------
COUNT     FORM      8
.-------------------------------------------------------
.@@@@@ Start of Generated Code @@@@@
. NB - Code & Comments Included Before the above line 
.      will not be overwritten by the generation of a 
.      program. The STDGENDF file and VERSION must appear
.      before the line. 
.-----------------------------------------
.   Program       :   IBASIN08
.   Function      :   Factor Maintenance                      
.   Generated  On :   13/12/2005
.-----------------------------------------
.=========================
.File Description Includes
.-------------------------
          INC       SINCIAFD/INC
          INC       SINCICFD/INC
          INC       SINFACFD/INC
.=========================
.Local Variable Definition
.-------------------------
.
ACCEPT    FORM      1
SACCEPT   FORM      1
.
PRGID     INIT      "IBASIN08"
PRGNAM    INIT      "Factor Maintenance                      "
.
.--------------------------------
. MAINLINE - Controlling Logic
.--------------------------------
          TOPIC     PRGID
          MOVE      PORT,PARENTID
          CLOCK     PORT,DIM2
          MOVE      DIM2,PORT
ML0000
          CALL      INIT0000
          CALL      SC00L000
          CHAIN     PGM
.---------------------------------------
. INIT - Open Files & Display Heading
.---------------------------------------
INIT0000  CALL      DISPHEAD
          MOVE      ONE,CCENTRY
          MOVE      ONE,CCANLDTE
          MOVE      ONE,CDEFDTE
          MOVE      ZERO,CHIGHLT
          DISPLAY   *P56:24,"Opening controlf";
          OPEN      CONTROLF,"controlf"
          DISPLAY   *P56:24,"Opening scrsbga1";
          OPEN      SCRSBGA1,"scrsbgaf"
          DISPLAY   *P56:24,"Opening scrpsta1";
          OPEN      SCRPSTA1,"scrpstaf"
          DISPLAY   *P56:24,"Opening scrpsta1";
          OPEN      SCRPSTA2,"scrpstaf"
          DISPLAY   *P56:24,"Opening scrslta1";
          OPEN      SCRSLTA1,"scrsltaf"
          MOVE      "   24",AUDTSECT
          MOVE      "205",AUDTPOST
          READ      CONTROLF,AUDTSECT;*AUDTPOST,AUDTFLAG
          IF        AUDTFLAG=1
            DISPLAY   *P56:24,"Opening sinaudfa";
            OPEN      SINAUDFA,"sinaudfa"
          ENDIF
          DISPLAY   *P56:24,"Opening sinciaaf";
          OPEN      SINCIAA1,"sinciaaf"
          DISPLAY   *P56:24,"Opening sinciaaf";
          OPEN      SINCIAA2,"sinciaaf"
          DISPLAY   *P56:24,"Opening sinciaaf";
          OPEN      SINCIAA3,"sinciaaf"
          DISPLAY   *P56:24,"Opening sincicaf";
          OPEN      SINCICA1,"sincicaf"
          DISPLAY   *P56:24,"Opening sincicaf";
          OPEN      SINCICA2,"sincicaf"
          DISPLAY   *P56:24,"Opening sincicaf";
          OPEN      SINCICA3,"sincicaf"
          DISPLAY   *P56:24,"Opening sinfacaf";
          OPEN      SINFACA1,"sinfacaf"
          DISPLAY   *P56:24,"Opening sinfacaf";
          OPEN      SINFACA2,"sinfacaf"
INIT9999  DISPLAY   *P1:24,*EL;
          RETURN
.
.------------------------------------
. Called for Print Screen Routine 
.------------------------------------
PRTSVARS
          MATCH     "00",SCRID
          IF         @EQUAL
            CALL       PS00L000   * Factor Maintenance                 
          ENDIF
          RETURN
.--------------------------------
. Screen - Factor Maintenance                 
.--------------------------------
SC00L000
SC00L100
          MOVE      "00",SCRID
          MOVE      ZERO,PRCOL
          MOVE      ZERO,PRVERT
          MOVE      "    4",FLDID
          CALL      GETPOSIT
          IF        EXIT=0
            MOVE     SCPSCOL,PRCOL
            MOVE     SCPSROW,PRVERT
          ENDIF
          CALL      SBACK000 * Display Background
.
SC00L200
          CALL      CF00L000 * Clear Fields
SC00L300
          CALL      KY00L000 * Enter Key Fields
          BRANCH    EXIT,SC00L999,SC00L999
SC00L400
          CALL      WP00L000 * Load any WP Details
SC00L500
          CALL      LM00L000 * Load any Batch Details
SC00L600
          CALL      DF00L000 * Display Fields
SC00L700
          CALL      SF00L000 * Select Fields
          BRANCH    EXIT,SC00L100,SC00L100,SC00L200
SC00L800
SC00L900
SC00L999  RETURN
.--------------------------------------------------------
. Clear Fields for Screen : Factor Maintenance                 
.-------------------------------------------------------
CF00L000
          MOVE      SP70,SIFAISSU
          MOVE      SP70,SIFASUPP
          MOVE      ZERO,SIFACTOR
CF00L999  RETURN
.--------------------------------------------------------
. Enter Key Fields for Factor Maintenance                 
.--------------------------------------------------------
KY00L000
.
KY00L110
          MOVE      "    1",FLDID
          CALL      GETPOSIT
          IF        EXIT=1
            MOVE      "Mandatory Field Missing. Fatal - ",DISPMSG
            BEEP
            CALL      MESSAGE1
            GOTO      FATALERR
          ENDIF
          MOVE      "2",CKEYTYP
          CALL      KSIFAA00
          BRANCH    EXIT,KY00L900,KY00L900
          MOVE      OVRCD,ACCEPT
          MOVE      SIFAISSU,VAR     
          CALL      DISPITEM
.
KY00L120
          MOVE      "    2",FLDID
          CALL      GETPOSIT
          IF        EXIT=1
            MOVE      "Mandatory Field Missing. Fatal - ",DISPMSG
            BEEP
            CALL      MESSAGE1
            GOTO      FATALERR
          ENDIF
          MOVE      "2",CKEYTYP
          CALL      ESUPP000
          BRANCH    EXIT,KY00L110,KY00L900
          MOVE      OVRCD,ACCEPT
          MOVE      SIFASUPP,VAR     
          CALL      DISPITEM
          MOVE      ZERO,EXIT
          GOTO      KY00L999
.
KY00L900  MOVE      ONE,EXIT
KY00L999  RETURN
.---------------------------------------------------------
. Load WP files for : Factor Maintenance                 
.---------------------------------------------------------
WP00L000
          RETURN
.---------------------------------------------------------
. Load Batch Screen for : Factor Maintenance                 
.---------------------------------------------------------
LM00L000
          RETURN
.---------------------------------------------------------
. Display Fields for : Factor Maintenance                 
.---------------------------------------------------------
DF00L000
          BRANCH   ACCEPT,DF00L999
          PACK     KEY15,PRGID,SCRID,SP70
          CALL     RSSCPS1
DF00L010
          CALL     RKSCPS1
          BRANCH   OVRCD,DF00L900
          MATCH    PRGID,SCPSPID
          GOTO     DF00L900 IF NOT EQUAL
          MATCH    SCRID,SCPSSID
          GOTO     DF00L900 IF NOT EQUAL
          MATCH    SP70,SCPSMTY
          GOTO     DF00L010 IF NOT EQUAL
.
          CALL     PS00L000 * Display Field
          GOTO     DF00L010
DF00L900
DF00L999
          RETURN
.---------------------------------------------------------
. Display/Print Fields for : Factor Maintenance                 
.---------------------------------------------------------
PS00L000   PACK      VAR,SP70,SP70
          PACK     PRINTVAR,SP70,SP70
          MOVE     SCPSITM,FIELDNO
          BRANCH    FIELDNO,PS00L999,PS00L999,PS00L101,PS00L999:
                            PS00L999,PS00L999,PS00L999
          GOTO     PS00L999
.
PS00L101
          MOVE      SIFACTOR,VAR     
          CALL      DISPITEM
          GOTO     PS00L999
PS00L999
          RETURN
.---------------------------------------------------------
. Selections for : Factor Maintenance                 
.---------------------------------------------------------
SF00L000  PACK     KEY13,PRGID,SCRID,SP70
          CALL     RSSCSL1
SF00L001  BRANCH   ACCEPT,SF00L020
.
SF00L005  CALL     MTSTQ000
          MOVE     SCSLITM,FIELDNO
          BRANCH    FIELDNO,SF00L006,SF00L006,SF00L101,SF00L006:
                            SF00L102,SF00L103,SF00L104
SF00L006  BEEP
          GOTO     SF00L005
SF00L020  CALL     RKSCSL1
          BRANCH   OVRCD,SF00L090
          MATCH    PRGID,SCSLPID
          GOTO     SF00L090 IF NOT EQUAL
          MATCH    SCRID,SCSLSID
          GOTO     SF00L090 IF NOT EQUAL
          MOVE     SCSLITM,FIELDNO
          BRANCH    FIELDNO,SF00L020,SF00L020,SF00L101,SF00L020:
                            SF00L020,SF00L020,SF00L020
          GOTO     SF00L020
SF00L090  MOVE     ZERO,ACCEPT
          GOTO     SF00L001
.
SF00L101
          CALL     SELECTED
          MOVE      "0",CKEYTYP
          CALL      EFACT000
          IF        SCPSMAN=1
            IF        EXIT=1
              MOVE      "Mandatory Field - ",DISPMSG
              BEEP
              CALL      MESSAGE1
              GOTO      SF00L101
            ENDIF
            BRANCH    EXIT,SF00L101,SF00L990
          ELSE
            COMPARE   TWO,EXIT
            GOTO      SF00L990 IF EQUAL
          ENDIF
          MOVE      SIFACTOR,VAR     
          CALL      DISPITEM
          GOTO     SF00L001
.
SF00L102
          CALL      POST0000
          MOVE      ONE,EXIT
          GOTO      SF00L999
.
SF00L103
          CALL      DELT0000
          MOVE      ONE,EXIT
          GOTO      SF00L999
.
SF00L104
          CALL      CANC0000
          MOVE      ONE,EXIT
          GOTO      SF00L999
SF00L990  MOVE      ONE,EXIT * Exit Char Restart
SF00L999  RETURN
.@@@@@ Function Restart Point @@@@@
ESUPP000  MOVE     SP70,SIFASUPP
          MOVE     "2",CKEYTYP
          CALL     KSIFAB00
          BRANCH   EXIT,ESUPP999,ESUPP999
          BRANCH   OVRCD,ESUPP999
          MOVE     TWO,AUDTTYPE
          CALL     WASIFA00          * Write Before Audit
ESUPP999  RETURN
EFACT000  MOVE      SP70,VAR
          CALL      DISPITEM
          IF       ACCEPT=1
            MOVE     ZERO,SIFACTOR
          ENDIF
          KEYIN     *PCCOL:CVERT,*V2LON,*RV,SIFACTOR:
                    *PCCOL:CVERT
          IF        SIFACTOR>0
            MOVE      ZERO,EXIT
          ELSE
            MOVE      "Factor Must be Greater Than Zero. - ",DISPMSG
            BEEP
            CALL      MESSAGE1
            GOTO      EFACT000
          ENDIF
          RETURN
POST0000
          PACK     KEY30,SIFAISSU,SIFASUPP
          CALL     RASIFA1                       * Supply/Issue Record
          IF       OVRCD=0
            CALL     UPSIFA1
            MOVE     THREE,AUDTTYPE
            CALL     WASIFA00
          ELSE
            CALL     WRSIFA1
            MOVE     ONE,AUDTTYPE
            CALL     WASIFA00
.
            PACK     KEY15,SIFASUPP              * Blank Record for Issue Unit
            PACK     KEY30,SIFAISSU,SP70
            UNPACK   KEY30,SIFAISSU,SIFASUPP
            CALL     RASIFA1
            IF       OVRCD=1
              MOVE     ONE,SIFACTOR
              CALL     WRSIFA1
            ENDIF
.
            PACK     KEY30,SP10,SP5,KEY15        * Blank Record for Supply Unit
            UNPACK   KEY30,SIFAISSU,SIFASUPP
            CALL     RASIFA1
            IF       OVRCD=1
              MOVE     ONE,SIFACTOR
              CALL     WRSIFA1
            ENDIF
.
          ENDIF
          RETURN

DELT0000
          CALL     CHKEX000                     * Check exist as valid comb.
          BRANCH   EXIT,DELT0300
.
          PACK     KEY30,SIFAISSU,SIFASUPP
          CALL     RASIFA1                       * Supply/Issue Record
          IF       OVRCD=0
            CALL     DASIFA00   * Delete Before Audit
            MOVE     FOUR,AUDTTYPE
            CALL     WASIFA00   * Write Audit Record
            CALL     DESIFA1
          ENDIF
          PACK     KEY15,SIFASUPP              * Blank Record for Issue Unit
          PACK     KEY30,SIFAISSU,SP70
          CALL     RDSIFA1
          BRANCH   OVRCD,DELT0200               * No on file
          CALL     RKSIFA1
          BRANCH   OVRCD,DELT0150               * On File but No Others
          MATCH    SIFAISSU,KEY30
          GOTO     DELT0200 IF EQUAL            * On File With Others
DELT0150  PACK     KEY30,SIFAISSU,SP70         * Delete if No Others
          CALL     RDSIFA1
          CALL     DESIFA1
DELT0200
          PACK     KEY30,SIFASUPP,SP70
          CALL     RDSIFA2
          BRANCH   OVRCD,DELT0300               * No on file
          CALL     RKSIFA2
          BRANCH   OVRCD,DELT0250               * On File but No Others
          MATCH    SIFASUPP,KEY30
          GOTO     DELT0300 IF EQUAL            * On File With Others
DELT0250  PACK     KEY30,SIFAISSU,SP70         * Delete if No Others
          CALL     RDSIFA2
          CALL     DESIFA2
DELT0300
          RETURN
.-----------------------------------------------------------------------
. Check if combination exists anywhere already
.-----------------------------------------------------------------------
CHKEX000  MOVE     ZERO,EXIT
          MOVE     ZERO,COUNT
          DISPLAY  *P1:24,*EL,"Checking Catalog/Supplier Factors. ";
          DISPLAY  *P1:23,*EL,"Records Processed :",COUNT
          PACK     KEY7,SP70
          CALL     RSSIIA1
CHKEX100  CALL     RKSIIA1
          BRANCH   OVRCD,CHKEX999
.
          ADD      ONE,COUNT
          IF       (COUNT%50)=0
            DISPLAY   *P20:23,COUNT
          ENDIF
.
          MATCH    SIFAISSU,SIIAISS
          GOTO     CHKEX100 IF NOT EQUAL
.
          PACK     KEY27,SIIACAT,SP70
          CALL     RSSIIC1
CHKEX200  CALL     RKSIIC1
          BRANCH   OVRCD,CHKEX100
.
          MATCH    SIIACAT,SIICCAT
          GOTO     CHKEX100 IF NOT EQUAL
.
          MATCH    SIICSUT,SIFASUPP
          GOTO     CHKEX200 IF NOT EQUAL
.
. ------- Found a valid combination
          DISPLAY  *P1:23,*EL,"Catalog/Supplier/Supplier Unit combination ":
                   SIICCAT,"/",SIICSUP,"/",SIICSUT," exists":
                   *P1:24,*EL,"for Supply/Issue factor combination entered. ";
          CALL     HITENTER
          MOVE     ONE,EXIT
.
CHKEX999  RETURN
CANC0000  CALL     DASIFA00
          RETURN
.-------------------------------
. Includes for I/O and Keyins
.-------------------------------
          INC       SINCIAIO/INC
          INC       SINCICIO/INC
          INC       SINFACIO/INC
          INC       SINFACKY    
.
          INC       STDGENCD
.
