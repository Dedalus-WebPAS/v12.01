.
. Include    :  WATSEIIO.INC
. File       :  watseiaf
. Keys       :  KEY13
.
RSWTSEI1  RESET     KEY13
          READ      WATSEIA1,KEY13;;
          RETURN
.
RAWTSEI1  RESET     KEY13
          MOVE      ZERO,OVRCD
          READ      WATSEIA1,KEY13;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RDWTSEI1  RESET     KEY13
          MOVE      ZERO,OVRCD
          READ      WATSEIA1,KEY13;WTSICONS,WTSILTYP,WTSIUSER,WTSISPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RKWTSEI1  MOVE      ZERO,OVRCD
          READKS    WATSEIA1;WTSICONS,WTSILTYP,WTSIUSER,WTSISPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RPWTSEI1  MOVE      ZERO,OVRCD
          READKP    WATSEIA1;WTSICONS,WTSILTYP,WTSIUSER,WTSISPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
UPWTSEI1  UPDATE    WATSEIA1;WTSICONS,WTSILTYP,WTSIUSER,WTSISPAR
          RETURN
.
WRWTSEI1  RESET     KEY13
          WRITE     WATSEIA1,KEY13;WTSICONS,WTSILTYP,WTSIUSER,WTSISPAR
          RETURN
.
DEWTSEI1  RESET     KEY13
          DELETE    WATSEIA1,KEY13
          RETURN
.
RSWTSEI2  RESET     KEY13
          READ      WATSEIA2,KEY13;;
          RETURN
.
RAWTSEI2  RESET     KEY13
          MOVE      ZERO,OVRCD
          READ      WATSEIA2,KEY13;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RDWTSEI2  RESET     KEY13
          MOVE      ZERO,OVRCD
          READ      WATSEIA2,KEY13;WTSICONS,WTSILTYP,WTSIUSER,WTSISPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RKWTSEI2  MOVE      ZERO,OVRCD
          READKS    WATSEIA2;WTSICONS,WTSILTYP,WTSIUSER,WTSISPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RPWTSEI2  MOVE      ZERO,OVRCD
          READKP    WATSEIA2;WTSICONS,WTSILTYP,WTSIUSER,WTSISPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
DEWTSEI2  RESET     KEY13
          DELETE    WATSEIA2,KEY13
          RETURN
.
