.
. PMSPITIO
. --------
. Filename     : pmspitaf.dat
.
RSPMPIT1  RESET     KEY36
          READ      PMSPITA1,KEY36;;
          RETURN
.
RAPMPIT1  RESET     KEY36
          MOVE      ZERO,OVRCD
          READ      PMSPITA1,KEY36;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RDPMPIT1  RESET     KEY36
          MOVE      ZERO,OVRCD
          READ      PMSPITA1,KEY36;PMPIURNO,PMPIATYP,PMPICLAM,PMPIFUND,PMPITABT:
                        PMPIEDAT,PMPIICNT,PMPIRINT,PMPIFDAT,PMPITDAT,PMPIDELT:
                        PMPICDAT,PMPICTIM,PMPICUID,PMPIUDAT,PMPIUTIM,PMPIUUID:
                        PMPIDDAT,PMPIDTIM,PMPIDUID,PMPISPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RKPMPIT1  MOVE      ZERO,OVRCD
          READKS    PMSPITA1;PMPIURNO,PMPIATYP,PMPICLAM,PMPIFUND,PMPITABT:
                        PMPIEDAT,PMPIICNT,PMPIRINT,PMPIFDAT,PMPITDAT,PMPIDELT:
                        PMPICDAT,PMPICTIM,PMPICUID,PMPIUDAT,PMPIUTIM,PMPIUUID:
                        PMPIDDAT,PMPIDTIM,PMPIDUID,PMPISPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RPPMPIT1  MOVE      ZERO,OVRCD
          READKP    PMSPITA1;PMPIURNO,PMPIATYP,PMPICLAM,PMPIFUND,PMPITABT:
                        PMPIEDAT,PMPIICNT,PMPIRINT,PMPIFDAT,PMPITDAT,PMPIDELT:
                        PMPICDAT,PMPICTIM,PMPICUID,PMPIUDAT,PMPIUTIM,PMPIUUID:
                        PMPIDDAT,PMPIDTIM,PMPIDUID,PMPISPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
UPPMPIT1  MOVE      ZERO,OVRCD
          UPDATE    PMSPITA1;PMPIURNO,PMPIATYP,PMPICLAM,PMPIFUND,PMPITABT:
                        PMPIEDAT,PMPIICNT,PMPIRINT,PMPIFDAT,PMPITDAT,PMPIDELT:
                        PMPICDAT,PMPICTIM,PMPICUID,PMPIUDAT,PMPIUTIM,PMPIUUID:
                        PMPIDDAT,PMPIDTIM,PMPIDUID,PMPISPAR
          GOTO      UPDFLOCK IF LOCKED
          RETURN
.
WRPMPIT1  RESET     KEY36
          WRITE     PMSPITA1,KEY36;PMPIURNO,PMPIATYP,PMPICLAM,PMPIFUND,PMPITABT:
                        PMPIEDAT,PMPIICNT,PMPIRINT,PMPIFDAT,PMPITDAT,PMPIDELT:
                        PMPICDAT,PMPICTIM,PMPICUID,PMPIUDAT,PMPIUTIM,PMPIUUID:
                        PMPIDDAT,PMPIDTIM,PMPIDUID,PMPISPAR
          RETURN
.
DEPMPIT1  RESET     KEY36
          DELETE    PMSPITA1,KEY36
          RETURN
.
RLPMPIT1  RESET     KEY36
          MOVE      ZERO,OVRCD
          READL     PMSPITA1,KEY36;PMPIURNO,PMPIATYP,PMPICLAM,PMPIFUND,PMPITABT:
                        PMPIEDAT,PMPIICNT,PMPIRINT,PMPIFDAT,PMPITDAT,PMPIDELT:
                        PMPICDAT,PMPICTIM,PMPICUID,PMPIUDAT,PMPIUTIM,PMPIUUID:
                        PMPIDDAT,PMPIDTIM,PMPIDUID,PMPISPAR
          GOTO      OVERCOND IF OVER
          GOTO      OVERLOCK IF LOCKED
          RETURN
.
KLPMPIT1  MOVE      ZERO,OVRCD
          READLKS   PMSPITA1;PMPIURNO,PMPIATYP,PMPICLAM,PMPIFUND,PMPITABT:
                        PMPIEDAT,PMPIICNT,PMPIRINT,PMPIFDAT,PMPITDAT,PMPIDELT:
                        PMPICDAT,PMPICTIM,PMPICUID,PMPIUDAT,PMPIUTIM,PMPIUUID:
                        PMPIDDAT,PMPIDTIM,PMPIDUID,PMPISPAR
          GOTO      OVERCOND IF OVER
          GOTO      OVERLOCK IF LOCKED
          RETURN
.
PLPMPIT1  MOVE      ZERO,OVRCD
          READLKP   PMSPITA1;PMPIURNO,PMPIATYP,PMPICLAM,PMPIFUND,PMPITABT:
                        PMPIEDAT,PMPIICNT,PMPIRINT,PMPIFDAT,PMPITDAT,PMPIDELT:
                        PMPICDAT,PMPICTIM,PMPICUID,PMPIUDAT,PMPIUTIM,PMPIUUID:
                        PMPIDDAT,PMPIDTIM,PMPIDUID,PMPISPAR
          GOTO      OVERCOND IF OVER
          GOTO      OVERLOCK IF LOCKED
          RETURN
.
UUPMPIT1  RESET     KEY36
          UNLOCK    PMSPITA1
          RETURN
.
