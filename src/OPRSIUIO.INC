. OPRSIUIO
.---------
.
.
. 1st Index
.
RSOPSIU1  RESET     KEY24
          READ      OPRSIUA1,KEY24;;
          RETURN
.
RAOPSIU1  RESET     KEY24
          MOVE      ZERO,OVRCD
          READ      OPRSIUA1,KEY24;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RDOPSIU1  RESET     KEY24
          MOVE      ZERO,OVRCD
          READ      OPRSIUA1,KEY24;OPSIITEM,OPSIDATE,OPSIUNIQ,OPSISPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RKOPSIU1  MOVE      ZERO,OVRCD
          READKS    OPRSIUA1;OPSIITEM,OPSIDATE,OPSIUNIQ,OPSISPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RPOPSIU1  MOVE      ZERO,OVRCD
          READKP    OPRSIUA1;OPSIITEM,OPSIDATE,OPSIUNIQ,OPSISPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
UPOPSIU1  UPDATE    OPRSIUA1;OPSIITEM,OPSIDATE,OPSIUNIQ,OPSISPAR
          RETURN
.
WROPSIU1  RESET     KEY24
          WRITE     OPRSIUA1,KEY24;OPSIITEM,OPSIDATE,OPSIUNIQ,OPSISPAR
          RETURN
.
DEOPSIU1  RESET     KEY24
          DELETE    OPRSIUA1,KEY24
          RETURN
.
.
. 2nd Index
.
RSOPSIU2  RESET     KEY24
          READ      OPRSIUA2,KEY24;;
          RETURN
.
RAOPSIU2  RESET     KEY24
          MOVE      ZERO,OVRCD
          READ      OPRSIUA2,KEY24;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RDOPSIU2  RESET     KEY24
          MOVE      ZERO,OVRCD
          READ      OPRSIUA2,KEY24;OPSIITEM,OPSIDATE,OPSIUNIQ,OPSISPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RKOPSIU2  MOVE      ZERO,OVRCD
          READKS    OPRSIUA2;OPSIITEM,OPSIDATE,OPSIUNIQ,OPSISPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RPOPSIU2  MOVE      ZERO,OVRCD
          READKP    OPRSIUA2;OPSIITEM,OPSIDATE,OPSIUNIQ,OPSISPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
UPOPSIU2  UPDATE    OPRSIUA2;OPSIITEM,OPSIDATE,OPSIUNIQ,OPSISPAR
          RETURN
.
WROPSIU2  RESET     KEY24
          WRITE     OPRSIUA2,KEY24;OPSIITEM,OPSIDATE,OPSIUNIQ,OPSISPAR
          RETURN
.
DEOPSIU2  RESET     KEY24
          DELETE    OPRSIUA2,KEY24
          RETURN
.
