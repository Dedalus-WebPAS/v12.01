.
. PMSSKIIO
. --------
. Filename     : pmsskiaf.dat
.
RSPMSKI1  RESET     KEY35
          READ      PMSSKIA1,KEY35;;
          RETURN
.
RAPMSKI1  RESET     KEY35
          MOVE      ZERO,OVRCD
          READ      PMSSKIA1,KEY35;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RDPMSKI1  RESET     KEY35
          MOVE      ZERO,OVRCD
          READ      PMSSKIA1,KEY35;PMSKCODE,PMSKKKWD,PMSKSPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RKPMSKI1  MOVE      ZERO,OVRCD
          READKS    PMSSKIA1;PMSKCODE,PMSKKKWD,PMSKSPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RPPMSKI1  MOVE      ZERO,OVRCD
          READKP    PMSSKIA1;PMSKCODE,PMSKKKWD,PMSKSPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
UPPMSKI1  MOVE      ZERO,OVRCD
          UPDATE    PMSSKIA1;PMSKCODE,PMSKKKWD,PMSKSPAR
          GOTO      UPDFLOCK IF LOCKED
          RETURN
.
WRPMSKI1  RESET     KEY35
          WRITE     PMSSKIA1,KEY35;PMSKCODE,PMSKKKWD,PMSKSPAR
          RETURN
.
DEPMSKI1  RESET     KEY35
          DELETE    PMSSKIA1,KEY35
          RETURN
.
RLPMSKI1  RESET     KEY35
          MOVE      ZERO,OVRCD
          READL     PMSSKIA1,KEY35;PMSKCODE,PMSKKKWD,PMSKSPAR
          GOTO      OVERCOND IF OVER
          GOTO      OVERLOCK IF LOCKED
          RETURN
.
KLPMSKI1  MOVE      ZERO,OVRCD
          READLKS   PMSSKIA1;PMSKCODE,PMSKKKWD,PMSKSPAR
          GOTO      OVERCOND IF OVER
          GOTO      OVERLOCK IF LOCKED
          RETURN
.
PLPMSKI1  MOVE      ZERO,OVRCD
          READLKP   PMSSKIA1;PMSKCODE,PMSKKKWD,PMSKSPAR
          GOTO      OVERCOND IF OVER
          GOTO      OVERLOCK IF LOCKED
          RETURN
.
UUPMSKI1  RESET     KEY35
          UNLOCK    PMSSKIA1
          RETURN
.
RSPMSKI2  RESET     KEY35
          READ      PMSSKIA2,KEY35;;
          RETURN
.
RAPMSKI2  RESET     KEY35
          MOVE      ZERO,OVRCD
          READ      PMSSKIA2,KEY35;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RDPMSKI2  RESET     KEY35
          MOVE      ZERO,OVRCD
          READ      PMSSKIA2,KEY35;PMSKCODE,PMSKKKWD,PMSKSPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RKPMSKI2  MOVE      ZERO,OVRCD
          READKS    PMSSKIA2;PMSKCODE,PMSKKKWD,PMSKSPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
RPPMSKI2  MOVE      ZERO,OVRCD
          READKP    PMSSKIA2;PMSKCODE,PMSKKKWD,PMSKSPAR
          GOTO      OVERCOND IF OVER
          RETURN
.
UPPMSKI2  MOVE      ZERO,OVRCD
          UPDATE    PMSSKIA2;PMSKCODE,PMSKKKWD,PMSKSPAR
          GOTO      UPDFLOCK IF LOCKED
          RETURN
.
WRPMSKI2  RESET     KEY35
          WRITE     PMSSKIA2,KEY35;PMSKCODE,PMSKKKWD,PMSKSPAR
          RETURN
.
DEPMSKI2  RESET     KEY35
          DELETE    PMSSKIA2,KEY35
          RETURN
.
RLPMSKI2  RESET     KEY35
          MOVE      ZERO,OVRCD
          READL     PMSSKIA2,KEY35;PMSKCODE,PMSKKKWD,PMSKSPAR
          GOTO      OVERCOND IF OVER
          GOTO      OVERLOCK IF LOCKED
          RETURN
.
KLPMSKI2  MOVE      ZERO,OVRCD
          READLKS   PMSSKIA2;PMSKCODE,PMSKKKWD,PMSKSPAR
          GOTO      OVERCOND IF OVER
          GOTO      OVERLOCK IF LOCKED
          RETURN
.
PLPMSKI2  MOVE      ZERO,OVRCD
          READLKP   PMSSKIA2;PMSKCODE,PMSKKKWD,PMSKSPAR
          GOTO      OVERCOND IF OVER
          GOTO      OVERLOCK IF LOCKED
          RETURN
.
UUPMSKI2  RESET     KEY35
          UNLOCK    PMSSKIA2
          RETURN
.
