RSPRTE1   RESET     KEY17
          READ      PRITEST1,KEY17;;
          RETURN
.
RAPRTE1   RESET     KEY17
          MOVE      ZERO,OVRCD
          READ      PRITEST1,KEY17;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RDPRTE1   RESET     KEY17
          MOVE      ZERO,OVRCD
          READ      PRITEST1,KEY17;PRTECODE,PRTEDATE,PRTEDESC,PRTEITMN:
                                  PRTESUBN,PRTEAMAN,PRTEMSER,PRTETESN,PRTEABRV
          GOTO      OVERCOND IF OVER
          RETURN
.
RKPRTE1   MOVE      ZERO,OVRCD
          READKS    PRITEST1;PRTECODE,PRTEDATE,PRTEDESC,PRTEITMN:
                                  PRTESUBN,PRTEAMAN,PRTEMSER,PRTETESN,PRTEABRV
          GOTO      OVERCOND IF OVER
          RETURN
.
RPPRTE1   MOVE      ZERO,OVRCD
          READKP    PRITEST1;PRTECODE,PRTEDATE,PRTEDESC,PRTEITMN:
                                  PRTESUBN,PRTEAMAN,PRTEMSER,PRTETESN,PRTEABRV
          GOTO      OVERCOND IF OVER
          RETURN
.
UPPRTE1   UPDATE    PRITEST1;PRTECODE,PRTEDATE,PRTEDESC,PRTEITMN:
                                  PRTESUBN,PRTEAMAN,PRTEMSER,PRTETESN,PRTEABRV
          RETURN
.
WRPRTE1   RESET     KEY17
          WRITE     PRITEST1,KEY17;KEY17,PRTEDESC,PRTEITMN:
                                  PRTESUBN,PRTEAMAN,PRTEMSER,PRTETESN,PRTEABRV
          RETURN
.
DEPRTE1   RESET     KEY17
          DELETE    PRITEST1,KEY17
          RETURN
.
RSPRTE2   RESET     KEY22
          READ      PRITEST2,KEY22;;
          RETURN
.
RDPRTE2   RESET     KEY22
          MOVE      ZERO,OVRCD
          READ      PRITEST2,KEY22;PRTECODE,PRTEDATE,PRTEDESC,PRTEITMN:
                                  PRTESUBN,PRTEAMAN,PRTEMSER,PRTETESN,PRTEABRV
          GOTO      OVERCOND IF OVER
          RETURN
.
RKPRTE2   MOVE      ZERO,OVRCD
          READKS    PRITEST2;PRTECODE,PRTEDATE,PRTEDESC,PRTEITMN:
                                  PRTESUBN,PRTEAMAN,PRTEMSER,PRTETESN,PRTEABRV
          GOTO      OVERCOND IF OVER
          RETURN
.
RPPRTE2   MOVE      ZERO,OVRCD
          READKP    PRITEST2;PRTECODE,PRTEDATE,PRTEDESC,PRTEITMN:
                                  PRTESUBN,PRTEAMAN,PRTEMSER,PRTETESN,PRTEABRV
          GOTO      OVERCOND IF OVER
          RETURN
.
.
RSPRTE3   RESET     KEY47
          READ      PRITEST3,KEY47;;
          RETURN
.
RDPRTE3   RESET     KEY47
          MOVE      ZERO,OVRCD
          READ      PRITEST3,KEY47;PRTECODE,PRTEDATE,PRTEDESC,PRTEITMN:
                                  PRTESUBN,PRTEAMAN,PRTEMSER,PRTETESN,PRTEABRV
          GOTO      OVERCOND IF OVER
          RETURN
.
RKPRTE3   MOVE      ZERO,OVRCD
          READKS    PRITEST3;PRTECODE,PRTEDATE,PRTEDESC,PRTEITMN:
                                  PRTESUBN,PRTEAMAN,PRTEMSER,PRTETESN,PRTEABRV
          GOTO      OVERCOND IF OVER
          RETURN
.
RPPRTE3   MOVE      ZERO,OVRCD
          READKP    PRITEST3;PRTECODE,PRTEDATE,PRTEDESC,PRTEITMN:
                                  PRTESUBN,PRTEAMAN,PRTEMSER,PRTETESN,PRTEABRV
          GOTO      OVERCOND IF OVER
          RETURN
.
