.----------------------------------------------------------------------
.  Clear ABF Admitted Acute Price weights
.
.  Modifications :
.
.  V10.14.00 02/04/2019 J.Tan       TSK 0857392
.            Created
.
.----------------------------------------------------------------------
CLPMSPWA  MOVE      SP70,PMPWCNTR  * Contract ID
          MOVE      SP70,PMPWDRGC  * DRG Code
          MOVE      SP70,PMPWDRGD  * DRG Description
          MOVE      SP70,PMPWSDPY  * Same-Day Payment List
          MOVE      SP70,PMPWBICU  * Bundle ICU
          MOVE      ZERO,PMPWALOS  * ALOS
          MOVE      ZERO,PMPWLOWB  * Lower bound (Inlier Bounds)
          MOVE      ZERO,PMPWUPPB  * Upper bound (Inlier Bounds)
          MOVE      ZERO,PMPWPSRV  * Private Patient Service(Adjust.)%
          MOVE      ZERO,PMPWPAED  * Paediatric (Adjustment)%
          MOVE      ZERO,PMPWSDAY  * Same Day (Price Weight)
          MOVE      ZERO,PMPWSSOB  * Short Stay Outlier Base (PW)
          MOVE      ZERO,PMPWSSOP  * Short Stay Outlier Per Diem (PW)
          MOVE      ZERO,PMPWINLI  * Inlier (Price Weight)
          MOVE      ZERO,PMPWLSOP  * Long Stay Outlier Per Diem (PW)
          MOVE      SP70,PMPWCDAT  * Date record created
          MOVE      SP70,PMPWCTIM  * Time record created
          MOVE      SP70,PMPWCUID  * Record created by
          MOVE      SP70,PMPWUDAT  * Date record updated
          MOVE      SP70,PMPWUTIM  * Time record updated
          MOVE      SP70,PMPWUUID  * Record updated by
          MOVE      SP70,PMPWSPAR  * Spare variable
          RETURN
.
