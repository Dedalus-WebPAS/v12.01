RSPRVA1   RESET     KEY16
          READ      PRIVAFD1,KEY16;;
          RETURN
.
RAPRVA1   RESET     KEY16
          MOVE      ZERO,OVRCD
          READ      PRIVAFD1,KEY16;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RDPRVA1   RESET     KEY16
          MOVE      ZERO,OVRCD
          READ      PRIVAFD1,KEY16;PRVAINVN,DPRVAUNQ,PRVACLAM,PRVASPAR
          GOTO      OVERCOND IF OVER
          MOVE      DPRVAUNQ,PRVAUNIQ
          RETURN
.
RKPRVA1   MOVE      ZERO,OVRCD
          READKS    PRIVAFD1;PRVAINVN,DPRVAUNQ,PRVACLAM,PRVASPAR
          GOTO      OVERCOND IF OVER
          MOVE      DPRVAUNQ,PRVAUNIQ
          RETURN
.
RPPRVA1   MOVE      ZERO,OVRCD
          READKP    PRIVAFD1;PRVAINVN,DPRVAUNQ,PRVACLAM,PRVASPAR
          GOTO      OVERCOND IF OVER
          MOVE      DPRVAUNQ,PRVAUNIQ
          RETURN
.
UPPRVA1   MOVE      PRVAUNIQ,DPRVAUNQ
          UPDATE    PRIVAFD1;PRVAINVN,DPRVAUNQ,PRVACLAM,PRVASPAR
          RETURN
.
WRPRVA1   RESET     KEY16
          MOVE      PRVAUNIQ,DPRVAUNQ
          WRITE     PRIVAFD1,KEY16;KEY16,PRVACLAM,PRVASPAR
          RETURN
.
DEPRVA1   RESET     KEY16
          DELETE    PRIVAFD1,KEY16
          RETURN
.
.         2nd Index
.
RSPRVA2   RESET     KEY16
          READ      PRIVAFD2,KEY16;;
          RETURN
.
RAPRVA2   RESET     KEY16
          MOVE      ZERO,OVRCD
          READ      PRIVAFD2,KEY16;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RDPRVA2   RESET     KEY16
          MOVE      ZERO,OVRCD
          READ      PRIVAFD2,KEY16;PRVAINVN,DPRVAUNQ,PRVACLAM,PRVASPAR
          GOTO      OVERCOND IF OVER
          MOVE      DPRVAUNQ,PRVAUNIQ
          RETURN
.
RKPRVA2   MOVE      ZERO,OVRCD
          READKS    PRIVAFD2;PRVAINVN,DPRVAUNQ,PRVACLAM,PRVASPAR
          GOTO      OVERCOND IF OVER
          MOVE      DPRVAUNQ,PRVAUNIQ
          RETURN
.
RPPRVA2   MOVE      ZERO,OVRCD
          READKP    PRIVAFD2;PRVAINVN,DPRVAUNQ,PRVACLAM,PRVASPAR
          GOTO      OVERCOND IF OVER
          MOVE      DPRVAUNQ,PRVAUNIQ
          RETURN
