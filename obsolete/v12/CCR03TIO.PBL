RSCCEX1   RESET     KEY16
          READ      CCSEXTA1,KEY16;;
          RETURN
.
RACCEX1   RESET     KEY16
          MOVE      ZERO,OVRCD
          READ      CCSEXTA1,KEY16;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RDCCEX1   RESET     KEY16
          MOVE      ZERO,OVRCD
          READ      CCSEXTA1,KEY16;CCEXDPT,CCEXSER,CCEXPCT,CCEXQTY:
                                  CCEXCST,CCEXBUD,CCEXNPT
          GOTO      OVERCOND IF OVER
          RETURN
.
RKCCEX1   MOVE      ZERO,OVRCD
          READKS    CCSEXTA1;CCEXDPT,CCEXSER,CCEXPCT,CCEXQTY:
                                  CCEXCST,CCEXBUD,CCEXNPT
          GOTO      OVERCOND IF OVER
          RETURN
.
RPCCEX1   MOVE      ZERO,OVRCD
          READKP    CCSEXTA1;CCEXDPT,CCEXSER,CCEXPCT,CCEXQTY:
                                  CCEXCST,CCEXBUD,CCEXNPT
          GOTO      OVERCOND IF OVER
          RETURN
.
UPCCEX1   UPDATE    CCSEXTA1;CCEXDPT,CCEXSER,CCEXPCT,CCEXQTY:
                                  CCEXCST,CCEXBUD,CCEXNPT
          RETURN
.
WRCCEX1   RESET     KEY16
          WRITE     CCSEXTA1,KEY16;CCEXDPT,CCEXSER,CCEXPCT,CCEXQTY:
                                  CCEXCST,CCEXBUD,CCEXNPT
          RETURN
.
DECCEX1   RESET     KEY16
          DELETE    CCSEXTA1,KEY16
          RETURN
.
RSCCEX2   RESET     KEY16
          READ      CCSEXTA2,KEY16;;
          RETURN
.
RACCEX2   RESET     KEY16
          MOVE      ZERO,OVRCD
          READ      CCSEXTA2,KEY16;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RDCCEX2   RESET     KEY16
          MOVE      ZERO,OVRCD
          READ      CCSEXTA2,KEY16;CCEXDPT,CCEXSER,CCEXPCT,CCEXQTY:
                                  CCEXCST,CCEXBUD,CCEXNPT
          GOTO      OVERCOND IF OVER
          RETURN
.
RKCCEX2   MOVE      ZERO,OVRCD
          READKS    CCSEXTA2;CCEXDPT,CCEXSER,CCEXPCT,CCEXQTY:
                                  CCEXCST,CCEXBUD,CCEXNPT
          GOTO      OVERCOND IF OVER
          RETURN
.
RPCCEX2   MOVE      ZERO,OVRCD
          READKP    CCSEXTA2;CCEXDPT,CCEXSER,CCEXPCT,CCEXQTY:
                                  CCEXCST,CCEXBUD,CCEXNPT
          GOTO      OVERCOND IF OVER
          RETURN
.
UPCCEX2   UPDATE    CCSEXTA2;CCEXDPT,CCEXSER,CCEXPCT,CCEXQTY:
                                  CCEXCST,CCEXBUD,CCEXNPT
          RETURN
.
WRCCEX2   RESET     KEY16
          WRITE     CCSEXTA2,KEY16;CCEXDPT,CCEXSER,CCEXPCT,CCEXQTY:
                                  CCEXCST,CCEXBUD,CCEXNPT
          RETURN
.
DECCEX2   RESET     KEY16
          DELETE    CCSEXTA2,KEY16
          RETURN
.
RDCCTM1   RESET     KEY32
          MOVE      ZERO,OVRCD
          READ      CCSTMPA1,KEY32;CCTMDPT,CCTMSER,CCTMPCT,CCTMEPS
          GOTO      OVERCOND IF OVER
          RETURN
.
WRCCTM1   RESET     KEY32
          WRITE     CCSTMPA1,KEY32;CCTMDPT,CCTMSER,CCTMPCT,CCTMEPS
          RETURN
.
