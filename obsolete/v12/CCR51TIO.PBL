RSCCTM1   RESET     KEY40
          READ      CCSTMPA1,KEY40;;
          RETURN
.
RACCTM1   RESET     KEY40
          MOVE      ZERO,OVRCD
          READ      CCSTMPA1,KEY40;ANS
          GOTO      OVERCOND IF OVER
          RETURN
.
RDCCTM1   RESET     KEY40
          MOVE      ZERO,OVRCD
          READ      CCSTMPA1,KEY40;CCTMSEQ,CCTMLV1,CCTMLV2,CCTMLV3
          GOTO      OVERCOND IF OVER
          RETURN
.
RKCCTM1   MOVE      ZERO,OVRCD
          READKS    CCSTMPA1;CCTMSEQ,CCTMLV1,CCTMLV2,CCTMLV3
          GOTO      OVERCOND IF OVER
          RETURN
.
RPCCTM1   MOVE      ZERO,OVRCD
          READKP    CCSTMPA1;CCTMSEQ,CCTMLV1,CCTMLV2,CCTMLV3
          GOTO      OVERCOND IF OVER
          RETURN
.
UPCCTM1   UPDATE    CCSTMPA1;CCTMSEQ,CCTMLV1,CCTMLV2,CCTMLV3
          RETURN
.
WRCCTM1   RESET     KEY40
          WRITE     CCSTMPA1,KEY40;CCTMSEQ,CCTMLV1,CCTMLV2,CCTMLV3
          RETURN
.
DECCTM1   RESET     KEY40
          DELETE    CCSTMPA1,KEY40
          RETURN
.
