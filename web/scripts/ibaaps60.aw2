BEGIN    { start=0
         }
//     { len=length($0)
           data=substr($0,2,len)
           if (start==0)
             { printf("%s\n",data)
               start=1
             }
           else
             { printf("%%%%[PAGE]%%%%[DOWN:100]%s\n",data)
             }
           next
         }
/  CONTINUED/ { print
                printf("%%%%[PAGE]%%%%[DOWN:100]\n")
               }
         { print
         }
