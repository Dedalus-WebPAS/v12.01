#!/bin/sh
#====================================================
# Script to decode base64 file created in HL7ORUMS
# Parameter - Filename to decode
#====================================================
mv $1 /tmp/$$.base64
base64 -di /tmp/$$.base64 >$1
rm /tmp/$$.base64

