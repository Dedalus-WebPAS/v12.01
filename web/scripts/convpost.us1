#!/bin/bash
scriptPath=`which $0`;
scriptDir="${scriptPath%/*}";

echo "CONVPOST - Postcode file Conversion"
echo "==================================="
echo " "
echo "Convert DHS SLA tab delimited file to fixed field file"
awk -f $scriptDir/convpost.awk $1 > postoutp.txt
